# ML Rules
- Problem: Supervised multiclass classification (Low, Medium, High, Critical).
- Model: DecisionTreeClassifier.
- Features: days_to_deadline, estimated_effort, business_impact, urgency, dependency_count, task_type.
- Tooling: Scikit-learn, Pandas, Joblib.
- The ML preprocessing used during prediction must remain consistent with preprocessing used during training.